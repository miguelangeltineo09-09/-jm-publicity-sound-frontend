// Configuración de PostCSS: procesa el CSS con los plugins de Tailwind
// (genera las utilidades) y Autoprefixer (agrega prefijos de navegador automáticamente).
const config = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};

export default config;
